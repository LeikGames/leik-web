---
interface Props {
    titleName: string
    qualiItems: Array<string>
    image: string
}

const { titleName, qualiItems, image } = Astro.props
---

<div>
    <img class="img-fluid" src={image} />
    <h2 class="mb-0 h5 poppins-bold">{titleName}</h2>
    <!-- <p> -->
    <!--     { -->
    <!--         qualiItems.map(function (item, idx) { -->
    <!--             return (idx ? ', ' : '') + item -->
    <!--         }) -->
    <!--     } -->
    <!-- </p> -->
    <slot />
</div>
